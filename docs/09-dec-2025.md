# Today's Update - December 9, 2025

## Authentication Improvements
- Added "Forgot Password" page (`/forgot-password`) with email reset functionality
- Added "Forgot your password?" link on login page
- Improved "Email not verified" error message on login

## Step 2 - Topics Improvements
- Added topic example for format guidance showing expected format
- Example: "Why Follow-Ups Fail When Reps Only 'Check In'—And a Repeatable Formula for Follow-Ups That Actually Move Deals Forward"

## Step 3 - Account Creation Updates
- Language options limited to English and French only
- Added Job Title field with info bubble tooltip ("The AI will draft articles as if it was doing this job")

## Google Sheets Integration Enhancements
- Customer-specific sheet is now created automatically when user signs up
- Topics from Step 2 are added to customer sheet with "Needs Draft" status
- Job field included in Main Sheet data
- Sheet columns: Topic, Status, Subject, Article, Last Update, Client

## User Flow & Onboarding Logic
- Added `onboarding_completed` flag tracking in user profile
- Middleware checks if onboarding is completed before allowing dashboard access
- Users without completed onboarding are redirected to Step 1
- Webhook sets `onboarding_completed = true` after successful payment

---

## Hydration Error Fix
Fixed React hydration mismatch on homepage caused by `Math.random()` and `Date.toLocaleTimeString()` generating different values on server vs client.
- Moved visitor counter and clock to client-side only using `useState` + `useEffect`

## Simplified User Flow - Get Started Only
Consolidated signup into the "Get Started" onboarding flow for a cleaner user journey.

### Changes:
- Removed standalone "Sign Up" link from homepage (desktop icon and quick links)
- `/signup` now redirects to `/articles/generate/step-1` (Get Started flow)
- Pricing page redirects non-authenticated users to Get Started flow instead of signup
- New flow: **Get Started → Define Style → Choose Topics → Create Account & Pay → Dashboard**

## Signup & Payment Flow Update
Updated the user registration flow to follow proper process: **Signup → Email Verification → Plan Selection → Payment → Dashboard**
### Flow Changes:
- After signup, users see email verification screen
- After email verification, users are redirected to `/subscribe` (not dashboard)
- After successful payment, users are redirected to `/dashboard` with success banner
- Removed trial period functionality (payment required immediately)


## Windows 95 UI Redesign - Complete

Transformed the entire application UI to match the classic Windows 95 aesthetic.

### Pages Updated:
- **Landing Page** - Personal homepage style with desktop icons, taskbar, visitor counter, and retro badges
- **Login/Signup** - Win95 dialog windows with beveled borders
- **Dashboard** - Win95 window with metric cards and style management
- **Article Generation Wizard** - 3-step wizard with Win95 tabs and progress bar
- **Article Styles** - List, detail, and edit pages with Win95 panels
- **Pricing** - Plan cards with Win95 groupbox styling
- **Billing** - Usage tracking with Win95 progress bars
- **Settings** - Account preferences with Win95 form controls

### Components Created:
Win95Window, Win95Button, Win95Input, Win95Textarea, Win95Select, Win95Checkbox, Win95Tabs, Win95Progress, Win95Badge, Win95Alert

### CSS Foundation:
- Win95 color palette variables
- Beveled border effects (raised/sunken)
- Responsive breakpoints for desktop, tablet, and mobile
